// Set this to your deployed backend base URL, e.g. "https://your-backend.example.com"
// A path is supported too, e.g. "https://example.com/myapp".
// Keep it empty to use the current site origin.
window.WHEEL_CONFIG = {
  apiOrigin: ""
};
